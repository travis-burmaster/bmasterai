# BMasterAI AgentCore — Environment Variables
# Copy to .env and fill in your values before running locally.

# ── Model ──────────────────────────────────────────────────────────────────────
# Bedrock model ID for the Strands agent
# Default: us.anthropic.claude-3-5-sonnet-20241022-v2:0
MODEL_ID=us.anthropic.claude-3-5-sonnet-20241022-v2:0

# ── AWS ────────────────────────────────────────────────────────────────────────
AWS_REGION=us-east-1
AWS_PROFILE=default   # or use AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY

# ── Optional: Bedrock Knowledge Base ───────────────────────────────────────────
# Set this to route research_topic() queries to your own KB
# If unset, falls back to Brave Search → DuckDuckGo
KNOWLEDGE_BASE_ID=

# ── Optional: Brave Search API ─────────────────────────────────────────────────
# Get a free key at https://api.search.brave.com
BRAVE_API_KEY=

# ── Set automatically by deploy.py ─────────────────────────────────────────────
# MEMORY_ARN is injected as an environment variable during deployment
MEMORY_ARN=
